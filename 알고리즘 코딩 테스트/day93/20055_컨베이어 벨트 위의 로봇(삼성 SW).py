# https://www.acmicpc.net/problem/20055# 로봇을 올리는 위치에 올리거나 로봇이 어떤 칸으로 이동하면# 그 칸의 내구도는 즉시 1만큼 감소한다.# 1. 벨트가 각 칸 위에 있는 로봇과 함께 한칸 회전한다.# 2. 가장 먼저 벨트에 올라간 로봇부터, 한칸 이동한다.# 만약 이동할 수 없다면 가만히 있는다.# 로봇이 이동하기 위해서는 이동하려는 칸에 로봇이 없으며, 그 칸의 내구도가 1 이상 남아 있어야 한다.# 올리는 위치에 있는 칸의 내구도가 0이 아니면 올리는 위치에 로봇을 올린다.# 내구도가 0인 칸의 개수가 K개 이상이라면 과정을 종료한다. 그렇지 않다면 1번으로 돌아간다.from collections import dequeimport sysinput = sys.stdin.readlineN, K = map(int, input().split())A = list(map(int, input().split()))belt = deque(A)          # 길이 2N (내구도)robot = deque([0] * N)   # 길이 N (로봇)step = 0while True:    step += 1    # 1. 회전    belt.rotate(1)    robot.rotate(1)    robot[-1] = 0    # 2. 로봇 이동    for i in range(N-2, -1, -1):        if robot[i] == 1 and robot[i + 1] == 0 and belt[i + 1] > 0:            robot[i] = 0            robot[i + 1] = 1            belt[i + 1] -= 1    robot[-1] = 0    # 3. 로봇 올리기    if belt[0] > 0 and robot[0] == 0:        robot[0] = 1        belt[0] -= 1    # 4. 종료 조건    if belt.count(0) >= K:        print(step)        break